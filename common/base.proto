syntax = "proto3";

package base;
option go_package = "./pb";

message ErrorDetail {
  // Error code
  int32 errorCode = 1;
  //Error message
  string message = 2;
  // Additional metadata associated with the Error
  map<string, string> metadata = 3;
}