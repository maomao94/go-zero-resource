syntax = "proto3";

package sys;
option go_package = "./pb";

message Empty {}

message PingResp{
  string msg = 1;
}

service sys {
  rpc ping (Empty) returns (PingResp);
  rpc login (Empty) returns (PingResp); // 登录
}